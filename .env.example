# Frontend-safe values only. Anything secret belongs in Worker secrets.

# Leave empty when the SPA and Worker are served from the same origin (default).
VITE_API_BASE_URL=

# Public Turnstile site key. Safe to expose; the secret key is a Worker secret.
VITE_TURNSTILE_SITE_KEY=
